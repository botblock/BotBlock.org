.card-content
    h1.is-size-4.has-text-grey-lighter#lists Get all lists' details
        a(href='#lists')  &supdsub;
    h6.is-size-6
        a(href='/api/lists', target='_blank', rel='nofollow')
            code GET /api/lists
    h5.is-size-5 Query String Parameters
    .table-container
        table.table.is-bordered.is-narrow
            thead
                tr
                    th Field
                    th Type
                    th Description
            tbody
                td filter
                td Boolean
                td If set to true, only API information fields will be returned for each list. Lists with no API data will not be included in the response.
    h5.is-size-5 Response Body
    .table-container
        table.table.is-bordered.is-narrow
            thead
                tr
                    th JSON Property
                    th Type
                    th Description
                    th Present when filtered
            tbody
                tr
                    td &lt;list_id&gt;
                    td Object
                    td The ID used in #{__('site_name')} for the list acts as the key for each property in the response.
                    td
                each prop in listProps
                    tr
                        td &lt;list_id&gt;.#{prop.prop}
                        td #{prop.type}
                        td #{prop.desc}
                        td
                            - checkbox = prop.prop.startsWith('api_') ? { type: 0, value: 1 } : { type: 2, value: 0 };
                            include ../../Includes/checkbox

    h5.is-size-5 Example Response
    h6.is-size-6
        a(href='/api/lists?filter=true', target='_blank', rel='nofollow')
            code GET /api/lists?filter=true
        pre
            code.
                {
                    "thelist.org": {
                        "api_docs": "https://thelist.org/api/docs",
                        "api_post": "https://thelist.org/api/bot/stats/:id",
                        "api_field": "server_count",
                        "api_shard_id": "shard_id",
                        "api_shard_count": "shard_count",
                        "api_shards": null,
                        "api_get": "https://thelist.org/api/bot/info/:id"
                    },
                    "listofbots.com": {
                        "api_docs": "https://listofbots.com/docs",
                        "api_post": "https://listofbots.com/api/stats/:id",
                        "api_field": "guild_count",
                        "api_shard_id": null,
                        "api_shard_count": null,
                        "api_shards": "shards",
                        "api_get": null
                    }
                }
